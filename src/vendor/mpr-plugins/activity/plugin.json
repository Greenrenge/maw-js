{
  "name": "activity",
  "version": "1.0.0",
  "entry": "./index.ts",
  "sdk": "^1.0.0",
  "description": "Classify pane activity by diffing follow/PTTY snapshots.",
  "cli": {
    "command": "activity",
    "help": "maw activity <pane> [--watch] [--json] [--window=<dur>] [--samples=N] | maw activity --all [--watch] [--json] [--window=<dur>] [--samples=N] - classify pane output as busy, idle, or stuck",
    "flags": {
      "--watch": "boolean",
      "--all": "boolean",
      "--json": "boolean",
      "--window": "string",
      "--samples": "string"
    }
  },
  "weight": 10,
  "license": "MIT",
  "schemaVersion": 1
}
